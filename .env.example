VITE_API_URL=

# Пример для запуска админки отдельно от прокси:
# VITE_API_URL=http://localhost:8000

# Токен на странице логина админки должен совпадать с ADMIN_API_TOKEN в tg_bot/.env (в VITE_ не кладём — только ввод в UI).
